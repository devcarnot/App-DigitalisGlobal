/** Project row columns for workspace shell — avoids `select('*')` on open. */
export const ERP_PROJECT_SHELL_COLUMNS =
  'id,name,description,description_attachments,project_type,project_type_ids,created_at,updated_at,start_date,deadline_date,board_column,client_name,lead_source,created_by,priority,deleted_at';
