/** Max tasks loaded into project workspace lists (most recent first). */
export const ERP_WORKSPACE_TASKS_MAX = 500;

/** Max tasks pulled for projects grid progress/sort (per chunk query). */
export const ERP_GRID_TASKS_PER_CHUNK_MAX = 250;

/** Max CRM leads returned in one admin list request. */
export const ERP_CRM_LEADS_MAX = 500;

/** Max workspace user rows in admin list API. */
export const ERP_ADMIN_USERS_MAX = 500;
